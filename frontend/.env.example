# Server-only Java origin. Browser calls the same-origin Next.js proxy by default.
EXPERIENCE_API_BASE_URL=http://127.0.0.1:8080
# Optional browser base override; direct cross-origin Java access requires separately configured CORS.
NEXT_PUBLIC_EXPERIENCE_API_BASE_URL=/api/experience
